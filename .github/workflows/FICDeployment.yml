name: Setup Federated Identity Credential
'on':
  workflow_dispatch:

jobs:
  DeployOIDC:
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.2.2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3.1.2
        with:
          terraform_version: latest

      - name: Az login
        uses: azure/login@v2.2.0
        with:
          creds: '{"clientId":"${{ secrets.AZURE_CLIENT_ID }}","clientSecret":"${{ secrets.AZURE_CLIENT_SECRET }}","subscriptionId":"${{ secrets.AZURE_SUBSCRIPTION_ID }}","tenantId":"${{ secrets.AZURE_TENANT_ID }}"}'
      
      - name: Verify WorkingDir
        run: pwd && ls -al ./terraform/fic

      - name: GH Identity
        run: |
          git config --global user.email "${{ secrets.GHUSEREMAIL }}"
          git config --global user.name "${{ secrets.GHUSER }}"
        
      - name: Create PLAN Directory
        run: |
          if [ ! -d "./terraform/fic/plans" ]; then
          mkdir -p ./terraform/fic/plans
          git add ./terraform/fic/plans
          git commit -m "Create PLAN Directory"
          git diff --cached --quiet || (git commit -m "Create PLAN Directory" && git push)
          fi

      - name: Initialize Terraform
        working-directory: ./terraform/fic
        run: terraform init

      - name: Validate Terraform Code
        working-directory: ./terraform/fic
        run: terraform validate

      - name: Terraform Plan
        working-directory: ./terraform/fic
        run: terraform plan -out=./plans/plan.tfplan | tee ./plans/plan_output.txt

      - name: Debug Plan Files
        run: ls -al ./terraform/fic/plans

      - name: Plan Summary
        uses: sgametrio/terraform-summary-action@main
        with:
          log-file: ./terraform/fic/plans/plan_output.txt
          title: "Terraform Plan Summary"

      - name: Apply FIC Configuration
        working-directory: ./terraform/fic
        run: terraform apply -auto-approve ./plans/plan.tfplan
